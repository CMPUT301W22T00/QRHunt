<?xml version="1.0" encoding="utf-8"?>
<shape  xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"
    android:padding="@dimen/padding">

    <solid
        android:color="@color/accent_light_blue_50op"
        />
    <stroke
        android:width="2dp"
        android:color="@color/accent_lilac"/>

    <corners
        android:bottomRightRadius="@dimen/padding"
        android:bottomLeftRadius="@dimen/padding"
        android:topLeftRadius="@dimen/padding"
        android:topRightRadius="@dimen/padding" />
</shape>
